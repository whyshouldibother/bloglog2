export type projectTable={
    id: number,
    title: string, 
    description?: string,
    creation?:string,
    lastUpdate?:string,
}
export type projectViewType={
    id: number,
    title: string,
    description?:string
}
